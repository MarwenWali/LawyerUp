// Placeholder API wrapper for future AI integration
const API = {
  baseUrl: '', // set by environment or server later
  async sendPrompt(prompt, options = {}) {
    // Implement API call to AI/service later
    // Return a mocked response for now
    return { text: `Mocked AI response for: ${prompt}` };
  },
};

export default API;
